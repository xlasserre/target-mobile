import { StyleSheet } from 'react-native';

import { red, regularFontSize } from '../../constants/styleConstants';

const styles = StyleSheet.create({
  loginError: {
    color: red,
    fontSize: regularFontSize,
    paddingTop: 5
  }
});

export default styles;
